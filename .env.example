# Database
DATABASE_URL="postgresql://postgres:Heejun12!@localhost:5432/aiproject"

# App
NEXT_PUBLIC_APP_URL="http://localhost:3000"
```

### 5. Vercel에서 프로젝트 import

1. Vercel 대시보드에서 "Add New" → "Project"
2. GitHub 저장소 선택
3. 프로젝트 설정:
   - **Framework Preset**: Next.js
   - **Build Command**: `npm run build` (또는 `npx prisma generate && npm run build`)
   - **Install Command**: `npm install`

### 6. Vercel Postgres 데이터베이스 생성

1. Vercel 프로젝트 대시보드 → "Storage" 탭
2. "Create Database" → "Postgres" 선택
3. 데이터베이스 이름 입력 후 생성
4. 자동으로 `DATABASE_URL` 환경변수 생성됨

### 7. 환경변수 설정

Vercel 프로젝트 → Settings → Environment Variables:
```
DATABASE_URL = (Vercel Postgres에서 자동 생성됨)
NEXT_PUBLIC_APP_URL = https://your-app.vercel.app